新要求，data里面的NIST是有原始数据jdx的，现在想做的是原始数据和图像一起跑，不过有个问题，我们的数据图StandardizedSpectra是以SDBS为主的，一个方案是NIST的原始数据和图像作为训练集，剩下的NIST没有的划分为测试集和验证集

对于新的数据集：

三种方法：

1. 已经的resent vgg vit等在新的划分上跑(纯图片)
2. resent vgg vit等加载预训练权重
3. 原始数据+图片跑：RNN LSTM GRU Transfomer，层数可以选 3层，7层，9层


结果分析：

1. Gadcam分析下不同气味类别下，哪些区域比较重要，能否找到一定规律
2. 图片的区域和原始数据对应的区域是否一致。
3. 最后还要对比原始数据+图片划分数据集和旧的纯图片数据集的方法
4. 最后做可视化吧，加载已经训练好的模型去测试
5. 总结所有实验的数据和绘制相关曲线，这步可以在我跑完实验后才做

data文件不要乱了，在standard光谱里面，是以SDBS的为主，就是对于一个谱图，SDBS有的是优先SDBS的，所以新划分的数据集中的测试和验证集可以在StandardizeSpectra里面直接拿出来用，然后测试集部分要重新利用NIST的原始数据画,保证训练图片全部和对上，此外，NIST里面的气象谱图要拉伸到和液体谱图差不多的形式，参考standardize.py的拉伸，因为气象原始数据集中在90-100%，和液相差距很大。文本数据也要处理，最好是处理成和图片对应的二维向量数据，频率范围是400-4000，参照standardize_spectra.py的处理，记得要拉伸气象谱图数据铺满。然后新的数据集要单独一个文件放置，里面分训练集文件(纯NIST)和测试集验证集文件(纯SDBS),就是和StandardizedSpectra这个数据集分开。


我的疑问：

1. 这样划分的话训练集是固定的，那测试和验证集是否是随机分配的，原来的纯图片是随机分配三者的吗
2. 重合部分，就是两者都有的图谱有1800种，能否利用起来这一特点呢，两者可能有细微的不同，因为是不同来源的谱图。


然后你对我的要求进行合理化设计

最后做个计划指导说明书，步骤讲解什么的。
