# 红外光谱图标准化方案

## 1. 数据现状

### 1.1 行号 <-> 文件名映射（已用 RDKit 分子式比对验证）

`data/smiles_spectrum_nist.csv` 第 1 行是表头，第 2 行开始是数据。
**第 N 行（含表头计数）对应图片文件 `N.png`**，即：

```
file_id = pandas_row_index(0-based) + 2
```

- `spectrum_type == 0`：该行没有 SDBS 样本，`data/SDBS/{file_id}.png` 不存在。
- `spectrum_type == 1` 或 `2`：`data/SDBS/{file_id}.png` 存在。
- `NIST == 1`：`data/NIST/{file_id}.jdx`（含原始数值数据）和 `data/NISTpng/{file_id}.png` 存在。

统计（共 8503 行）：

| 来源 | 行数 |
|---|---|
| spectrum_type=1（SDBS 类型1） | 1767 |
| spectrum_type=2（SDBS 类型2，目标标准） | 592 |
| NIST=1 且 spectrum_type=0（只有 NIST，无 SDBS） | 544 |
| NIST=1 且 spectrum_type∈{1,2}（两者都有，优先用 SDBS） | 1153 |
| 两者都没有 | 5600 |

最终能产出标准化光谱图的行数 = 592 + 1767 + 544 = **2903** 行。

### 1.2 三种图片样式的实际差异

| | 类型1 (SDBS) | 类型2 (SDBS，标准) | NIST |
|---|---|---|---|
| 尺寸 | 715×553（固定） | 800×441（固定） | 1600×1200（来自 svg 渲染，可重渲染为任意尺寸） |
| 内容 | 信息框(HIT-NO/SDBS-NO) + 化合物名 + 分子式 + 谱图 + 峰值表 + 结构图 | 只有坐标轴+曲线+左下角一行版权小字 | 标题文字 + 谱图 + 底部版权文字 |
| X 轴 | Wavenumber, 4000→约400 cm⁻¹ | Wavenumber, 4000→400 cm⁻¹ | Wavenumber (cm-1)，范围/留白与前两者不同 |
| Y 轴 | %T, 0-100 | %T, 0-100 | **Relative Transmittance, 0-1**（刻度体系不同） |
| 曲线颜色 | 黑色 | 黑色 | 红色 |
| 原始数值数据 | **已丢失**（SDBS 文件夹已被替换为纯 png） | 已丢失 | **仍保留**（`data/NIST/*.jdx`） |

## 2. 标准化目标

以**类型2**的视觉规范作为标准：纯白底、黑色曲线、只有坐标轴框+刻度，X 轴 4000→400 cm⁻¹（从左到右递减），Y 轴 0-100 %T，无标题、无版权文字、无多余内容。最终图片统一命名为 `{file_id}.png`，与 csv 行号一一对应，输出到新文件夹（如 `data/Standardized/`）。

## 3. 三类数据各自的处理方式

### 3.1 类型2（592个）—— 直接复用
已经是标准格式，只需：
- 校验所有类型2图片尺寸/坐标轴范围确实一致（抽样已确认 800×441 固定）。
- 直接拷贝到输出目录，重命名为 `{file_id}.png`（其实文件名已经是 file_id，无需改名）。

### 3.2 类型1（1767个）—— 裁剪而非重绘
原始数值数据已丢失，只能在像素层面操作现有 PNG：
1. **自动检测绘图区边框**：类型1是固定模板（715×553 不变），用图像处理找出谱图面板的黑色矩形边框（扫描每行/列像素，找到分隔信息框/谱图/峰值表的水平黑线，定位绘图区 bounding box），而不是硬编码像素坐标——更稳妥，万一模板有细微版本差异也能适应。
2. 裁剪出绘图区（只保留坐标轴框内的曲线部分，去掉信息框、峰值表、结构图）。
3. 核对裁剪后的坐标轴范围与类型2一致（都是 4000-400 cm⁻¹ / 0-100 %T），如果一致，可直接缩放（resize）到与类型2相同的画布尺寸；如果坐标范围有细节差异（比如右边界是400还是500），需要先按比例裁掉多余部分再缩放，避免直接 resize 导致坐标轴比例失真。
4. 去掉裁剪后图像上残留的坐标轴文字（"WAVENUMBER"等标签如果在裁剪范围内被保留，需要确认类型2是否也保留——从截图看类型2也保留了 "Wavenumber [cm-1]" 和 "%T" 标签，所以类型1裁剪时应保留同样的标签区域，不要切太狠）。

### 3.3 NIST-only（544个）—— 用原始数值重新画图
这部分仍有 `data/NIST/{file_id}.jdx` 原始光谱数值（`##XYDATA`），是最干净的处理方式：
1. 解析 jdx 的 `##XUNITS`/`##YUNITS`/`##XFACTOR`/`##YFACTOR`/`##FIRSTX`/`##DELTAX`/`##XYDATA`，还原出真实的 (wavenumber, transmittance) 数值序列。
2. 统一单位：
   - X 轴：若 `XUNITS=MICROMETERS`，转换成 cm⁻¹（`cm-1 = 10000 / micrometers`）。
   - Y 轴：若 `YUNITS=ABSORBANCE`，转换成 %T（`%T = 10^(2-A)`，即 `T = 10^-A` 后转百分比）；若是 Reflectance/absorption index/dispersion index 等少数特殊类型（实测中占比很小），物理意义不同，不能强行转换，建议直接剔除或单独标记，不纳入标准化数据集。
3. 用 matplotlib 按统一模板重新绘图：黑色曲线、白底、X 轴 4000→400 cm⁻¹、Y 轴 0-100 %T、坐标轴标签样式与类型2一致，不画标题、不画版权文字，画布尺寸与类型1/2裁剪后的输出尺寸保持一致。
4. 这一步同时也是检验"裁剪类型1"那条路线是否靠谱的交叉验证手段：如果 NIST-only 和类型1/2 在同一批重复样本（1153 个两者都有的）上重新画出来的曲线趋势吻合，说明坐标系理解是对的。

## 4. 待确认 / 风险点

1. **类型1裁剪后的坐标轴范围是否与类型2严格一致**——需要抽样核对（比如有的化合物右边界是400，有的可能是450/500），这会影响能不能直接统一 resize。
2. **少数非 Transmittance/Absorbance 的 Y 轴单位**（Reflectance、absorption index、dispersion index，共约70个文件，来自 SDBS 原始 jdx——但这些 jdx 现在还在不在 SDBS 文件夹？需要确认，如果已经被替换成 png 且对应的恰好是类型1/2，可能已经没法分辨，需要决定是否剔除这批数据）。
3. **重复的1153行（SDBS与NIST都有）**：按你的要求优先用SDBS，但建议保留一份"该行 SDBS 类型(1/2)"和"是否有NIST备选"的记录，方便后续如果发现SDBS裁剪质量不好时还能回退用NIST重绘的版本。
4. 最终输出文件夹结构、是否要在 csv 里新增一列记录"最终图片来源类型"（1/2/NIST-redrawn/none），建议加，方便追溯。

## 5. 建议的实施顺序

1. 写脚本生成「行号 -> file_id -> 最终来源类型」的映射表（csv 新增列），先把账算清楚，不碰图片。
2. 处理类型2（最简单，零风险，验证流程通畅）。
3. 处理 NIST-only 重绘（有原始数值，质量可控，且能反过来验证类型1裁剪是否合理）。
4. 处理类型1裁剪（最复杂，需要先用少量样本试裁剪+人工目视核对，再批量跑）。
5. 全部完成后，抽样对比三类来源的最终输出图，确认坐标轴范围、字体、线宽、画布尺寸视觉上统一。
